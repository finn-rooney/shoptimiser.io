---
export interface Props {
    logo: string;
    alt: string;
    width: number;
}

const { logo, alt, width } = Astro.props
---

<div class="flex items-center justify-center">
    <img src={logo} alt={alt} width={width + "px"} />
</div>
